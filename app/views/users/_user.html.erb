<li>
  <%= gravatar_for user, size: 52 %>
  <%= link_to user.name, user %>
  <% if current_user.admin and !current_user?(user) and !user.admin %>
    | <%= link_to "delete", user, method: :delete, confirm: "You sure?" %>
  <% end %>

</li>